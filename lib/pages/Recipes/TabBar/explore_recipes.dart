import 'package:flutter/material.dart';

class ExploreRecipes extends StatelessWidget {
  const ExploreRecipes({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Explorar recetas (en construcción)'),
    );
  }
}
